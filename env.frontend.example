# Frontend/Relayer environment (copy to .env.local)

# Local RPC (Hardhat)
LOCAL_RPC=http://127.0.0.1:8545

# Optional Testnet RPC (Sepolia)
# TESTNET_RPC=

# Deployed contracts (from Hardhat deploy output)
NEXT_PUBLIC_TOKEN_ADDRESS=
NEXT_PUBLIC_GASLESS_ADDRESS=

# Optional API key header to protect relayer route
# RELAYER_API_KEY=

# Relayer private key (server-side only; DO NOT commit .env.local)
RELAYER_PRIVATE_KEY=


